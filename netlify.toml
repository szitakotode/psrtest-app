[build]
  # This is where your serverless functions will live
  functions = "netlify/functions"

# This rule makes it so when your frontend calls /api/buttons,
# Netlify runs the 'buttons.js' function.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200